const URL = "https://concept.research.microsoft.com/api/Concept/ScoreByProb?instance=apple&topK=5"

export const process = (query) => {
  tokens = query.trim().split(' ');
  for (token of tokens) {
    xhr.open("GET", "/bar/foo.txt", true);
  }
}
